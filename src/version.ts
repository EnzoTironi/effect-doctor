export const VERSION = "0.2.0";

/** Nome no npm. `effect-doctor` já está publicado por outro autor. */
export const PACKAGE_NAME = "effect-ts-doctor";

/** Binário instalado (`npx effect-ts-doctor`). */
export const CLI_BIN = "effect-ts-doctor";

/** Dist-tag `effect@rc` resolvido em 2026-09-15. */
export const TARGET_EFFECT_RC = "4.0.0-rc.115";

/** Versão do relatório `--json`. Consumidores ramificam neste campo. */
export const JSON_SCHEMA_VERSION = 1;
