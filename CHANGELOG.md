# Changelog

## 0.2.0

- Alvo pinado em `effect@4.0.0-rc.115`.
- 177 regras com receita ruim→bom e `explain`.
- Nits (info) sempre ligados; score ignora nits.
- Relatório `--json` com `schemaVersion` e `id` estável por diagnóstico.
- Licença Modified MIT (mesmo recorte do react-doctor: sem treino de IA, sem SaaS pago sem permissão).
- Pacote npm: `effect-ts-doctor` (`effect-doctor` no registry já pertence a outro autor). Bin `effect-ts-doctor`. `SEE LICENSE IN LICENSE`, `exports`, CI, dogfood, corpus público.

## 0.1.0

- Primeira fatia do CLI (AST, fixtures, Alchemy estático).
